Status: current
Owner: frontend
Last reviewed: 2026-09-06

# MARU 홈 디자인 기준

이 문서는 `/`에 구현한 시네마틱 홈의 시각 기준이다. 제품 목적은 `PRODUCT.md`, 브랜드 문맥은 `.impeccable.md`, 쇼핑과 관리자 화면의 공통 규칙은 `docs/design-system.md`를 따른다. 현재 코드의 기록이며 프로덕션 승인이나 최종 완료를 뜻하지 않는다.

## 방향과 범위

큰 한국어 제목, 사진을 가로지르는 MARU 워드마크, 스크롤에 반응하는 사진으로 공간과 제품을 발견하는 경험을 만든다. 기존 아이보리와 적갈색, 한국어 명조 서체, 실제 API 사진을 사용한다. 홈의 연출을 검색, 결제, 주문, 관리자 화면의 기본 동작으로 확장하지 않는다.

구현 기준은 `apps/storefront/src/app/(store)/page.tsx`, `apps/storefront/src/components/home/cinematic-home.tsx`, `apps/storefront/src/app/globals.css`의 `cinema-` 클래스다.

## 색과 서체

| 역할 | 기존 토큰 | 값 |
| --- | --- | --- |
| 아이보리 바탕 | `--background` | `oklch(0.976 0.012 82)` |
| 밝은 표면 | `--surface` | `oklch(0.992 0.006 82)` |
| 본문과 소재 이야기 배경 | `--ink` | `oklch(0.205 0.018 47)` |
| 보조 설명 | `--ink-soft` | `oklch(0.405 0.018 51)` |
| 제목과 주요 링크의 적갈색 | `--primary` | `oklch(0.405 0.105 28)` |
| 구분선 | `--line` | `oklch(0.872 0.016 76)` |
| 키보드 초점 | `--focus` | `oklch(0.54 0.145 28)` |

제목과 워드마크는 Gowun Batang, 본문과 조작부는 Noto Sans KR을 사용한다. 좁은 화면의 홈 제목은 `clamp(2.4rem, 4.7vw, 4.5rem)`과 최대 `13ch`, 워드마크는 `clamp(7rem, 27vw, 25rem)`을 사용한다. 데스크톱에서는 제목을 `clamp(3rem, 4.3vw, 4.25rem)`, 사진 안의 워드마크를 `clamp(7rem, 15vw, 15rem)`으로 조정한다. 주요 설명은 0.875rem과 1.9 행간이다. 사진과 가는 구분선, 여백으로 위계를 만들고 소재 이야기만 짙은 배경으로 구분한다.

## 홈의 구성

1. 첫 화면: 제목, 설명, 주요 링크, 공간 사진과 워드마크. 계절 상품과 컬렉션이 있을 때 해당 영역으로 바로 가는 링크를 제공한다.
2. 브랜드 관점: `m.` 표식과 편안함, 소재, 형태를 설명하는 짧은 문장.
3. 공간 컬렉션: API의 최대 세 카테고리를 번호, 사진, 설명, 실제 카테고리 링크로 소개한다.
4. 계절의 선택: 최대 네 상품. 모바일과 태블릿은 두 열, 1024px 이상은 네 열이다. 상품별 높이 오프셋 없이 사진 상단을 맞추고, 이름 길이가 달라도 가격과 구매 조작부를 같은 행의 하단에 정렬한다.
5. 최근 본 상품이 있으면 기존 최근 본 상품과 추천 영역을 이어서 표시한다.
6. 소재 이야기: 활성 프로모션의 사진, 제목, 설명, 링크를 사용한다.
7. 신상품 최대 네 개와 주문 상태 확인, 비회원 주문 조회, 쇼핑 도움말 링크로 마무리한다.

홈의 섹션 간격은 `clamp(5rem, 8vw, 7rem)`이다. 전시 숨김 설정과 빈 데이터에 따라 섹션을 생략하며, 상품과 가격은 기존 API 계약을 따른다.

| 화면 너비 | 첫 화면 | 공간 컬렉션 | 상품 |
| --- | --- | --- | --- |
| 768px 미만 | 제목, 설명, 사진 순서 | 한 열 | 두 열 |
| 768px 이상, 1024px 미만 | 소개 두 열 아래에 사진 | 두 열, 첫 카테고리는 두 칸을 쓰는 리드 행 | 두 열 |
| 1024px 이상 | 소개와 사진을 약 39:61로 나란히 배치 | 1.2:1:1의 세 열, 첫 사진은 세로 비율, 가운데 장면에 높이 차이 | 네 열 |

이 배열은 JavaScript, 화면 높이, 모션 감소 설정과 무관하다. 기존의 긴 sticky 장면과 SSR 정적 대체 사이의 구조 차이를 없앴다. 콘텐츠의 DOM 순서와 키보드 탐색 순서는 유지한다.

데스크톱 컬렉션은 사진의 비율과 시작 높이 차이를 유지하면서 세 열을 같은 높이로 늘린다. 설명 영역의 남은 공간을 사용해 탐색 링크를 공통 하단에 정렬한다. 제목과 설명은 각 열 안에서 줄바꿈하며 모바일과 태블릿의 기존 흐름에는 적용하지 않는다.

## 모션과 정적 대체

큰 연출은 화면 너비 1024px 이상, 높이 700px 이상이며 `prefers-reduced-motion: no-preference`일 때만 활성화한다. 홈에 한정해 공통 140-240ms 모션 규칙의 예외를 둔다.

- 히어로 사진은 스크롤에 따라 확대되고 세로로 이동하며, 워드마크는 가로로 이동한다. 마우스 포인터에만 작은 원근 반응을 적용한다.
- 컬렉션은 각 사진 프레임 안에서 위치와 배율만 스크롤에 연결한다. 사진마다 이동량에 작은 차이를 주고 번호, 설명, 링크 및 열 배치는 고정한다. 중복 이미지와 sticky 전환은 사용하지 않는다.
- 소재 사진은 스크롤에 따라 배율과 위치가 변한다. 제목과 링크를 숨기지 않는다.
- 제목 진입은 800ms, 계절 상품 진입은 700ms에 상품마다 60ms 지연, 링크 화살표 반응은 350ms다. 반복 재생되는 장식은 없다.
- 서버 렌더와 모션 비활성 상태에서도 동일한 반응형 배열과 카테고리 사진을 보여준다. 좁거나 낮은 화면은 스크롤 확대와 포인터 원근 반응을 사용하지 않는다.
- 모션 감소 설정은 홈의 CSS 애니메이션과 전환도 제거한다. 브라우저의 기본 스크롤과 앵커 탐색을 유지한다.

## 접근성과 콘텐츠

홈의 주요 링크와 버튼은 최소 44px 조작 영역을 사용한다. 링크의 키보드 초점은 `--focus` 외곽선으로 표시하고, 상품의 구매 조작부는 마우스를 올리지 않아도 보인다. 장식 워드마크는 보조 기술에서 제외하고, 카테고리 사진에는 API의 대체 텍스트를 사용한다. 앵커는 대상 섹션이 있을 때만 제공한다.

사진은 기존 API 자료다. 사진이 실제 상품명이나 소재 설명과 일치하는지 별도로 확인해야 한다. 새로운 제품 사양, 후기, 실적이나 서비스 약속을 만들지 않는다.

## 검증 상태

2026-09-06 작업 검증 기록 기준이다. 실제 컴포넌트를 서버에서 렌더한 정적 프리뷰를 1440px과 390px에서 시각 확인했다. 390px, 768px, 1280px, 1440px에서 이미지 13개가 모두 로드되었고, 가로 넘침이 없으며 주요 링크 11개가 최소 44px인 것을 확인했다. 상품명 링크 8개도 390px에서 최소 44px로 재확인했다.

후속 데스크톱 개선은 실제 사용자 뷰포트 1201x1280에서 확인했다. 컬렉션 높이는 약 2299px에서 859px, 전체 문서 높이는 7123px에서 5355px로 줄었다. 별도 Chromium 정적 검증은 390, 768, 1024, 1201, 1440, 1920px에서 가로 넘침 없음, 예상 열 수, 카테고리 Tab 순서, 모션 감소 설정 전후 같은 배열을 확인했다. 근거는 `.omx/desktop-layout-results.json`이다.

독립 레이아웃 재검토는 지정된 정적 캡처와 코드 범위에서 새 중대한 문제 없이 승인 가능한 것으로 판정했다. 린트, TypeScript를 포함한 프로덕션 빌드, 홈 데이터 계약 fixture 7건도 통과했다. 이 판정은 아래의 미검증 통합 범위를 포함하지 않는다.

정적 프리뷰는 실제 API 연결과 hydration 검증을 대체하지 않는다. 통합 E2E의 실행 상태는 작업 브랜치의 GitHub Actions CI 결과를 별도로 확인한다. 로컬 API 실행 주소 조정은 승인 대기이며, 수동 모션 품질과 프레임 성능은 미검증이다. 일부 상품명과 사진의 불일치가 남아 있다. 접근성 전체 적합성과 프로덕션 품질, PM 최종 승인은 확정하지 않는다.

상품 배열의 후속 수정에서는 엇갈림을 제거했다. 여섯 화면 너비의 정적 검증에서 같은 행의 사진 상단, 가격 하단, 담기 버튼 하단이 일치했고 가로 넘침이 없었다. 해당 조건을 통합 E2E에도 추가했다.
